#include <bits/stdc++.h>

using namespace std;

int main(){
    //ios_base::sync_with_stdio(false);
    //cin.tie(NULL);

    long long unsigned int a, b;
    cin>>a>>b;
    long long unsigned int soma;
    soma = ((a + b)*( b - a + 1))/2;
    cout<<soma<<"\n";
}